# ===================================
# BACKEND API ENVIRONMENT VARIABLES
# ===================================
# Copy this file to .env.production and fill in your values
# DO NOT COMMIT .env.production to git!

# Database URL - From Neon.tech
# Must include ?sslmode=require for Neon
DATABASE_URL="postgresql://username:password@ep-xxx.region.neon.tech/dbname?sslmode=require"

# JWT Configuration
# Generate a strong random string (min 32 characters)
# Use: openssl rand -base64 32
JWT_SECRET="your-super-secret-production-key-change-this-min-32-chars"
JWT_EXPIRES_IN=7d

# Server Configuration
PORT=5001
NODE_ENV=production

# CORS - Your Vercel frontend URL
# Get this AFTER deploying frontend to Vercel
# Must match exactly (include https://, no trailing slash)
CORS_ORIGIN=https://your-app-name.vercel.app

# ===================================
# INSTRUCTIONS:
# ===================================
# 1. Create Neon database and get connection string
# 2. Generate strong JWT_SECRET: openssl rand -base64 32
# 3. Set these in Render dashboard Environment Variables
# 4. After deploying frontend, update CORS_ORIGIN with Vercel URL
# 5. Redeploy backend to apply CORS changes
